<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper">
    <select id="selectList" resultType="com.todo.demo.data.Todo">
      SELECT title, priority,deadline, isComplete
      FROM todotable
      WHERE todoNo = #{num};
    </select>

    <select id="selectTodoNo" resultType="Integer">
      SELECT MAX(todoNo)
      FROM todotable;
    </select>

    <insert id="insertTodo">
        INSERT INTO todotable(title, content, deadline, priority, isComplete)
        VALUES(#{title}, #{content}, #{deadline}, 1, false);
    </insert>

    <select id="selectTodo" resultType="com.todo.demo.data.Todo">
        SELECT title, content, deadline, isComplete
        FROM todotable
        WHERE todoNo = #{num};
    </select>
</mapper>